/**
 * Common size variants used across UI components.
 */
export type Size = 'xs' | 'sm' | 'md' | 'lg' | 'xl';

/**
 * Common visual variants used for buttons, badges, etc.
 */
export type Variant = 'primary' | 'secondary' | 'outline' | 'danger' | 'ghost';

/**
 * Status types used for badges, notifications, and state indicators.
 */
export type Status = 'success' | 'warning' | 'danger' | 'info' | 'neutral';
